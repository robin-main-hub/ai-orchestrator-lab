# Handoff — 2026-06-15 (밤) → 2026-06-16 (회사 접속)

오늘 친 작업을 회사 환경에서 이어받을 때 필요한 컨텍스트만 압축. 본문 안 읽어도
**"내일 첫 5분"** 섹션만 봐도 다시 들어갈 수 있게.

---

## 내일 첫 5분

1. `git pull origin main` — 마지막 head는 `b98db1b` (SimpleMemo 잔재 정리)
2. **남은 한 줄 (선택)**: dev 서버 안 도는 상태에서
   ```bash
   git mv packages/memory packages/simplememo
   git commit -m "chore(rename): packages/memory dir → packages/simplememo"
   git push
   ```
   디렉토리 이름까지 simplememo로 통일하는 마무리. 어제 dev watcher 잠금으로 못 했음.
   *동작에는 영향 없음* — pnpm workspace는 dir 이름이 아닌 package.json의 `name`을 봄.
3. 그다음 작업은 H8 — 아래 "Next candidates" 참고

---

## 어제 닫힌 큰 흐름 (App Builder loop)

```
Conversation → AppBuild → Mission Workspace
  → Generated Files Inspector (H3)
  → Preview iframe 임베드 (PreviewRunCard + ChatSidePanel "미리보기" 탭)
  → Preview 좌표 캡처 클릭 (H7 P2) → pendingCoords
  → Preview Annotator (H7) → annotation
  → Turbo Edits prompt 빌더 (H5) — annotation/AppFixDraft/instruction을 prompt로
  → in-app provider 호출 (H6) — selectedProvider/selectedModel 재사용
  → validate → SearchReplaceEditCard (H4) 자동 주입
  → 사용자 Apply → ScaffoldOverlay
  → Preview rerun → Visual QA rerun → Evidence Card
```

전 단계가 한 화면에서 자연스럽게 흘러갑니다. 외부 LLM 복붙 없음. 자동 실행 0.

---

## OSS-Harvest 진행 상황

| ID | 내용 | 상태 | 커밋 |
|---|---|---|---|
| H1 | shadcn/ui Card+Badge+Button + VisualEvidenceCard swap | ✅ | `5b0445f` |
| H2 | Octokit adapter (W5c PR read/update만) | ✅ | `d25885b` |
| H3 | Generated Files Inspector (Dyad-급, 자체 구현) | ✅ | `9df198e` |
| H4 | Search/Replace Edit Bridge → ScaffoldOverlay | ✅ | `3833721` |
| H5 | Turbo Edits Prompt Bridge | ✅ | `10e73d6` |
| H6 | In-app provider 호출 다리 | ✅ | `e6bbecb` |
| H7 | Preview Annotator (텍스트) | ✅ | `b1fa32e` |
| H7 P2 | iframe 좌표 캡처 + Turbo Edits 합류 | ✅ | `990564d` |
| — | PreviewRunCard inline iframe + ChatSidePanel preview 탭 | ✅ | `fa94072` |
| — | activePreviewRef 배선 (App.tsx까지 lift) | ✅ | `4108d60` |
| H8 | OpenCode runner adapter spike | 🟡 후보 |
| 추가 | shadcn primitives 더 swap (StatusBar/ProgressRail) | 🟡 후보 |

---

## SimpleMemo rename (어제 마지막 vertical)

`@ai-orchestrator/memory` → **`@ai-orchestrator/simplememo`**. 커밋 `cf3f6d6`, 잔재 정리 `b98db1b`.

**변경된 이름 매핑** (코드에서 새 이름 써야 함):

| 옛 이름 | 새 이름 |
|---|---|
| `@ai-orchestrator/memory` | `@ai-orchestrator/simplememo` |
| `DgxSimpleMemMemoryAdapter` | `SimpleMemAdapter` |
| `MockMemoryAdapter` | `MockAdapter` |
| `MockMemoryAdapterOptions` | `MockAdapterOptions` |
| `dgxSimpleMemAdapter.ts` | `simpleMemAdapter.ts` |
| `mockMemoryAdapter.ts` | `mockAdapter.ts` |

**유지된 이름** (의도적):
- `MementoMcpAdapter` — 외부 Memento MCP 의존성을 이름에 분명히
- `LocalHeuristicAdapter`
- `withTrustEnforcement`, `TrustPolicy`
- 인터페이스 타입 `MemoryAdapter`/`MemoryRecord`/`RecallQuery` 등 — **도메인 어휘** (SimpleMemo는 구현 브랜드, 두 층 분리)

~~**아직 남은 한 가지**: 디렉토리 이름 `packages/memory`~~ → **2026-06-16 완료**: `git mv packages/memory packages/simplememo` + `apps/desktop/vite.config.ts` alias 경로 정정 + lockfile 재생성. typecheck/build/1745 desktop·171 simplememo 테스트 회귀 0.

**docs는 의도적으로 그대로**: `docs/32-memory-adapter-spec.md`(헤더에 새 이름 안내 추가) + `docs/work-board.md`(work log, 역사적 정확성) — 본문은 spec 작성 당시 이름 보존.

---

## 검증 결과 (어제 마지막 측정)

- `pnpm typecheck` (root: protocol → providers → simplememo → agents → server → desktop) — **clean**
- `packages/memory` (= simplememo 패키지) — **171/171 tests green**
- `apps/desktop` — **277 files / 1642 tests green** (rename으로 인한 0 회귀)
- `apps/server` — **521 pass / 38 fail** — 38 fail은 모두 W1~W4 frozen-clock TTL **baseline** (사용자 사전 인지·승인된 비차단). 이번 rename으로 새로 깨진 거 0.

---

## 알려진 baseline 실패 (server, 무관 항목)

W1c smoke / W2 branch create / W3b file change execute / W4b PR create execute 계열 약 38 tests — frozen-clock 환경에서 TTL이 만료되어 approval/execute 흐름이 막힘. 사용자가 이전에 **비차단**으로 명시 승인. 이 항목으로 새 작업 막지 마세요.

---

## 어제 안 한 것 (의도적 미룸)

- H7 좌표 캡처는 cross-origin 안전한 *오버레이 클릭 좌표만* 캡처. **selector/text DOM 읽기는 0**. iframe DOM 접근 필요해지면 postMessage 다리를 직접 깔아야 함 (scaffold 템플릿에 inject) — 큰 작업이라 별도 vertical.
- App.tsx에서 ConversationWorkbench로 가는 `previewUrl`은 **활성 preview observed 시에만** 갱신. preview_not_running/error에선 가짜로 안 갱신. mission 전환 시 stale URL 표시될 수 있는데 — 자주 발생하면 mission별 ref 보관하는 식으로 한 줄 다듬으면 됨.
- shadcn 도입은 `Card`/`Badge`/`Button` + 3 컴포넌트만. ProgressRail / StatusBar / PreviewRunCard / Annotator는 BEM 그대로. 점진 swap이 안전.

---

## Next candidates (우선순위 추천)

1. **`packages/memory` → `packages/simplememo` 디렉토리 rename 마무리** (dev 멈춘 상태에서 1줄, ~10초)
2. **App.tsx의 activePreviewRef를 missionId별로** — mission 전환 시 stale URL 회피
3. **H8: OpenCode runner adapter spike** — 사용자 directive에 있던 그 다음 OSS harvest
4. **추가 shadcn swap**: MissionWorkspaceStatusBar / AppBuildProgressRail
5. **Server baseline 38개 fix** — frozen-clock TTL 진짜 fix (별 vertical, 큰 작업)

---

## 환경/접근 메모

- 워킹 디렉토리: `C:\Users\Robin\ai-orchestrator-lab`
- 원격: `github.com/robin-main-hub/ai-orchestrator-lab` (`origin/main`)
- dgx-02: Tailscale 접근 (auto memory `ai-orchestrator-lab-project.md` 참고)
- **공개 저장소** — 인프라 정보(IP/hostname 등) 커밋 금지

---

## 작업 스타일 리마인더 (사용자 본인 mandate)

- 한 번에 사용자 체감 slice → 마지막에 테스트 1회 → 커밋 1개
- 작업 단위 너무 잘게 X / 여러 작업 한 번에 묶기
- 자동 실행 0 / GitHub write는 의도된 vertical에서만 / fake observed 금지
- 새 server route / 새 npm dep은 vertical 의도에 정확히 일치할 때만

---

오늘 푹 쉬시고, 내일 회사에서 봬요. 🌙
